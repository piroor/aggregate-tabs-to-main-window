{
  "name": "aggregate-tabs-to-main-window",
  "version": "0.0.0",
  "engines": {
    "node": ">=8.6.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "eslint": "^9.15.0",
    "jsonlint-cli": "*",
    "tunnel-agent": ">=0.6.0"
  },
  "devDependencies": {
    "globals": "^15.12.0"
  }
}
